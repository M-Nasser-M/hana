{
  "name": "aws-sst",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "export AWS_PROFILE=hana && sst dev",
    "build": "export AWS_PROFILE=hana && sst build",
    "deploy": "export AWS_PROFILE=hana && sst deploy",
    "remove": "export AWS_PROFILE=hana && sst remove",
    "console": "export AWS_PROFILE=hana && sst console",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {},
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "valibot": "^0.30.0",
    "@tsconfig/node18": "^18.2.2",
    "aws-cdk-lib": "2.124.0",
    "constructs": "10.3.0",
    "sst": "^2.40.6",
    "typescript": "^5.4.2"
  }
}